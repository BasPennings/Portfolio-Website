/*
    Idea language switching
    - in HTML add lang param to text.
    - Create text in nl and en, also add lang for each tag.
    - Set display to none for lang(nl) if language selector
      is on en.
    - Set display to none for lang(en) if language selector
      is on nl.
*/